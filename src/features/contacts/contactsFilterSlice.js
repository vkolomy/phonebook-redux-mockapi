// import { createSlice } from '@reduxjs/toolkit'

// const initialState = {
//     filteredContacts: [],
// }

// export const contactsFilterSlice = createSlice({
//   name: 'contactsFilter',
// initialState,
//   reducers: {
//   //   dynamicSearch(state, action) {
//   //     console.log("action dynamic search: ", action)
//   //     state.filteredContacts = action.payload == '' ? state.contacts : state.contacts.filter(contact => contact.name.toLowerCase().includes(action.payload.toLowerCase()))      
//   //  },
      
//   }
// })

// // Action creators are generated for each case reducer function
// export const { dynamicSearch } = contactsFilterSlice.actions

// export default contactsFilterSlice.reducer